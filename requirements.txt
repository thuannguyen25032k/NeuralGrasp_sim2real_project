absl-py==0.15.0
einops==0.3.2
ftfy
gdown==3.13.1
matplotlib
numpy
pandas==1.4.1
pyrender==0.1.45
regex
scipy
einops
transformers==4.3.2
trimesh==3.9.34
h5py==3.3.0
# hydra-core version is pinned to 1.1 in the Dockerfile (final install wins).
# The sweeper/launcher plugins are kept at a compatible version.
hydra-core==1.2.0
hydra-optuna-sweeper==1.2.0
hydra-submitit-launcher==1.2.0
tqdm
zarr
diffusers
kornia
open3d
av==13.0.0  # PyAV — required by torchvision.io.write_video; >=14 breaks pict_type setter (needs int enum, not string)